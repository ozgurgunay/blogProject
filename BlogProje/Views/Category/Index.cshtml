
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model CategoryVM


@*<div class="container">
    <div class="row">
        <div class="col-3">
            <div class="list-group">
                @foreach (CategoryVM category in Model.)
                {
                    @Html.ActionLink(category.CategoryId, "Index", "Category", new { id = category.CategoryId }, new { @class = "list-group-item list-group-item-action" })
                }
            </div>

        </div>
    </div>
</div>*@